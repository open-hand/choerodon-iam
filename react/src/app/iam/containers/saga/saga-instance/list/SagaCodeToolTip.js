import React, { useContext } from 'react';

import Store from '../store';

export default function SagaCodeToolTip(props) {
  const { sagaDetailDataSet } = useContext(Store);
  return (
    <div>
      aaa
    </div>
  );
}
