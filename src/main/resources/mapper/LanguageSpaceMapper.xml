<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.choerodon.base.infra.mapper.LanguageSpaceMapper">
    <select id="fulltextSearch" resultType="io.choerodon.base.infra.dto.LanguageSpaceDTO">
        SELECT *
        FROM fd_prompt
        <where>
            <if test="languageSpaceDTO.promptCode != null">
                AND prompt_code LIKE concat(concat('%',#{languageSpaceDTO.promptCode}),'%')
            </if>
            <if test="languageSpaceDTO.lang != null">
                AND lang LIKE concat(concat('%',#{languageSpaceDTO.lang}),'%')
            </if>
            <if test="languageSpaceDTO.description != null">
                AND description LIKE concat(concat('%',#{languageSpaceDTO.description}),'%')
            </if>
            <if test="languageSpaceDTO.serviceCode != null">
                AND service_code LIKE concat(concat('%',#{languageSpaceDTO.serviceCode}),'%')
            </if>
            <if test="param != null">
                AND (
                prompt_code LIKE concat(concat('%',#{param}),'%') OR
                lang LIKE concat(concat('%',#{param}),'%') OR
                description LIKE concat(concat('%',#{param}),'%') OR
                service_code LIKE concat(concat('%',#{param}),'%')
                )
            </if>
        </where>
    </select>
</mapper>
