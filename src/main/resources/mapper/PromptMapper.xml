<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.choerodon.base.infra.mapper.PromptMapper">
    <select id="fulltextSearch" resultType="io.choerodon.base.infra.dto.PromptDTO">
        SELECT *
        FROM fd_prompt
        <where>
            <if test="promptDTO.promptCode != null">
                AND prompt_code LIKE concat(concat('%',#{promptDTO.promptCode}),'%')
            </if>
            <if test="promptDTO.lang != null">
                AND lang LIKE concat(concat('%',#{promptDTO.lang}),'%')
            </if>
            <if test="promptDTO.description != null">
                AND description LIKE concat(concat('%',#{promptDTO.description}),'%')
            </if>
            <if test="promptDTO.serviceCode != null">
                AND service_code LIKE concat(concat('%',#{promptDTO.serviceCode}),'%')
            </if>
            <if test="param != null">
                AND (
                prompt_code LIKE concat(concat('%',#{param}),'%') OR
                lang LIKE concat(concat('%',#{param}),'%') OR
                description LIKE concat(concat('%',#{param}),'%') OR
                service_code LIKE concat(concat('%',#{param}),'%')
                )
            </if>
        </where>
    </select>
</mapper>
